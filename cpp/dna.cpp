#include<bits/stdc++.h>
using namespace std;
bool comp(int a, int b)
{
    return (a < b);
}

int main()
{
    string s; cin>>s;
    int a=0,c=0,g=0,t=0;
    // for(int i = 0;i<s.length();i++)
    // {
    //      if(s[i] == 'A') a++;
    //      if(s[i] == 'C') c++;
    //      if(s[i] == 'G') g++;
    //      if(s[i] == 'T') t++;
    // }
    // cout<<max({a,c,g,t},comp);

    for(int i=0;i<s.length();i++)
    {
         
    }

}
