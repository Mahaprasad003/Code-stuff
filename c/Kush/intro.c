#include<stdio.h>

int main()
{
     int a;
     scanf("%d", &a);
     printf("%d", a);








     //HELLO
     // char ch[10];
     // scanf(" %c", &ch);
     // int length = 0;
     // for(int i=0;ch[i]!='\0';i++)
     // {
     //      length++;
     // }
     // ch[length] = '\0';
     // // for(int i=0;ch != '\0'; i++)
     // // {
     // //      printf(ch[i]);
     // // }
     // printf("%s", ch);
}
